services:
  api:
    build:
      context: ./apps/api
      dockerfile: Dockerfile
    container_name: kin-api
    environment:
      PORT: 8080
    ports:
      - "8080:8080"
    restart: unless-stopped